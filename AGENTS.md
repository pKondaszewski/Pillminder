# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v56.0.0/ before writing any code.
Read commit messages rules at https://www.conventionalcommits.org/en/v1.0.0/ before commiting any code.

# Code style

Order by importance, public first. In every file the exported (`export`)
functions go above private helpers — the public API reads first, the
implementation details below it.
